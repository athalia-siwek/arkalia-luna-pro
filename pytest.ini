[pytest]
minversion = 6.0

# ✅ Options unifiées
addopts = --strict-markers -ra --tb=short --cov=modules --cov=arkalia --cov-report=html

# ✅ Répertoires des tests
testpaths =
    tests/unit
    tests/integration

# ✅ Marqueurs personnalisés
markers =
    asyncio: marque pour tests asynchrones (asyncio)
    slow: marque pour tests longs ou gourmands
    performance: marque pour tests de performance
    unit: marque pour tests unitaires
    integration: marque pour tests d'intégration
